# 18 — Student UI Screen Map

## Public
`/`

## Auth
`/login`, `/signup`, `/forgot-password`

## Onboarding
`/onboarding/academic-profile`, `/onboarding/branch`, `/onboarding/semester`, `/onboarding/final-setup`

## Student app
`/chat`, `/chat?conversation=id`, `/subjects`, `/subjects/[id]`, `/library`, `/profile`, `/settings`

## Settings
`/settings`, `/settings/account`, `/settings/academic`, `/settings/preferences`, sign-out modal.

## Edge states
Chat loading, insufficient source, answer error, rate limit, session expired, no subjects, subject coming soon, copy success, feedback success, mobile drawer.
